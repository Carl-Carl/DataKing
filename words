按照1->4的顺序，将语句存入Request对象中
1. main words
    create "from" "set"
    select "set" from "from" (where "where") + (orderby "order")
    update "from" set "set" + (where "where")
    insert into "from" ("set") values ("values")
    delete from "from" (where "where")
    drop "from"

2. file level
    from

3. consult level
    set
    values
    where

4. present level
    order by

main -> from(file) -> where -> 

create class age=int, score = double, name = string
